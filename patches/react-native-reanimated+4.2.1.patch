diff --git a/node_modules/react-native-reanimated/lib/module/hook/useAnimatedStyle.js b/node_modules/react-native-reanimated/lib/module/hook/useAnimatedStyle.js
index dfdf30d..6c421cb 100644
--- a/node_modules/react-native-reanimated/lib/module/hook/useAnimatedStyle.js
+++ b/node_modules/react-native-reanimated/lib/module/hook/useAnimatedStyle.js
@@ -415,14 +415,17 @@ For more, see the docs: \`https://docs.swmansion.com/react-native-reanimated/doc
       jestAnimatedValues,
       toJSON: animatedStyleHandleToJSON,
       styleUpdaterContainer
-    } : __DEV__ ? {
-      get _requiresAnimatedComponent() {
-        throw new ReanimatedError('Perhaps you are trying to pass an animated style to a non-animated component. Try creating an animated component using `createAnimatedComponent` function or use `Animated.*` components.');
-      },
+    } : __DEV__ ? Object.defineProperty({
       viewDescriptors,
       initial,
       styleUpdaterContainer
-    } : {
+    }, '_requiresAnimatedComponent', {
+      get() {
+        throw new ReanimatedError('Perhaps you are trying to pass an animated style to a non-animated component. Try creating an animated component using `createAnimatedComponent` function or use `Animated.*` components.');
+      },
+      enumerable: false,
+      configurable: true
+    }) : {
       viewDescriptors,
       initial,
       styleUpdaterContainer
diff --git a/node_modules/react-native-reanimated/src/hook/useAnimatedStyle.ts b/node_modules/react-native-reanimated/src/hook/useAnimatedStyle.ts
index 59f036d..5f4f783 100644
--- a/node_modules/react-native-reanimated/src/hook/useAnimatedStyle.ts
+++ b/node_modules/react-native-reanimated/src/hook/useAnimatedStyle.ts
@@ -609,16 +609,23 @@ For more, see the docs: \`https://docs.swmansion.com/react-native-reanimated/doc
           styleUpdaterContainer,
         }
       : __DEV__
-        ? ({
-            get _requiresAnimatedComponent() {
-              throw new ReanimatedError(
-                'Perhaps you are trying to pass an animated style to a non-animated component. Try creating an animated component using `createAnimatedComponent` function or use `Animated.*` components.'
-              );
-            },
-            viewDescriptors,
-            initial,
-            styleUpdaterContainer,
-          } as AnimatedStyleHandle<Style | AnimatedProps>)
+        ? Object.defineProperty(
+            {
+              viewDescriptors,
+              initial,
+              styleUpdaterContainer,
+            } as AnimatedStyleHandle<Style | AnimatedProps>,
+            '_requiresAnimatedComponent',
+            {
+              get() {
+                throw new ReanimatedError(
+                  'Perhaps you are trying to pass an animated style to a non-animated component. Try creating an animated component using `createAnimatedComponent` function or use `Animated.*` components.'
+                );
+              },
+              enumerable: false,
+              configurable: true,
+            }
+          )
         : {
             viewDescriptors,
             initial,
